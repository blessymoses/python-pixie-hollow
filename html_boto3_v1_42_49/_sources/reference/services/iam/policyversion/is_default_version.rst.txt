:doc:`PolicyVersion <index>` / Attribute / is_default_version

******************
is_default_version
******************



.. py:attribute:: IAM.PolicyVersion.is_default_version

  

  - *(boolean) --* 

    Specifies whether the policy version is set as the policy's default version.

    